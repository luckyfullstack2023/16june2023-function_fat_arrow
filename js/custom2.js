

// There are Two way to define a function

// 1.(A) old way (1995-2014)
    // I.define
    function aFunction(){    //classic style
        console.log("Hello from aFunction");
        
    }
    // II.call
    aFunction();

//2.(B) New style 2015 ES-6
    var bFunction = () =>{      //Fat arrow function
        console.log('Hello from bFunction');
        
    }
    bFunction();
// 3.(C) old way (1995-2014)
    // I.define
    function cFunction(){    //classic style
        console.log(`Hello from cFunction`);
        
    }
    // II.call
    cFunction();

//4.(D) New style 2015 ES-6
    var dFunction = () =>{      //Fat arrow function
        console.log("Hello from dFunction");
        
    }
    dFunction();
// 5.(E).old way (1995-2014)
    // I.define
    function eFunction(){   //clasic style
        console.log('Hello from eFunction');
    }
    // II.call
    eFunction();

// 6.(F).New style 2015 ES-6
    let fFunction = () =>{      //Fat arrow function
        console.log("Hello from dFunction");
        
    }
    fFunction();
// 7.(G).old way (1995-2014)
    // I.define
    function gFunction(){   //clasic style
        console.log('Hello from gFunction');
    }
    // II.call
    gFunction();

// 8.(H).New style 2015 ES-6
    let hFunction = () =>{      //Fat arrow function
        console.log("Hello from hFunction");
        
    }
    hFunction();
// 9.(I).old way (1995-2014)
    // I.define
    function iFunction(){   //clasic style
        console.log(`Hello from iFunction`);  
    }
    // II.call
    iFunction();

// 10.(J).New style 2015 ES-6
    const jFunction = () =>{      //Fat arrow function
        console.log("Hello from jFunction");
        
    }
    jFunction();


// 11.(K).old way(1995-2014)
    // I.define
    function kFunction(){   //clasic style
        console.log(`Hello from kFunction `);  
    }
    // II.call
    kFunction();


// 12.(L).New style 2015 ES-6
    var lFunction = () =>{      //Fat arrow function
        console.log('Hello from lFunction');
        
    }
    lFunction();

// 13.(M).old way(1995-2014)
    // I.define
    function mFunction(){   //clasic style
        console.log("Hello from mFunction ");  
    }
    // II.call
    mFunction();


// 14.(N).New style 2015 ES-6
    let nFunction = () =>{      //Fat arrow function
        console.log(`Hello from nFunction`);
        
    }
    nFunction();

// 15.(O).old way(1995-2014)
    // I.define
    function oFunction(){   //clasic style
        console.log('Hello from oFunction ');  
    }
    // II.call
    oFunction();


// 16.(P).New style 2015 ES-6
    const pFunction = () =>{      //Fat arrow function
        console.log("Hello from pFunction");
        
    }
    pFunction();

// 17.(Q).old way(1995-2014)
    // I.define
    function qFunction(){   //clasic style
        console.log(`Hello from qFunction `);  
    }
    // II.call
    qFunction();


// 18.(R).New style 2015 ES-6
    var rFunction = () =>{      //Fat arrow function
        console.log('Hello from rFunction');
        
    }
    rFunction();

// 19.(S).old way(1995-2014)
    // I.define
    function sFunction(){   //clasic style
        console.log("Hello from sFunction ");  
    }
    // II.call
    sFunction();


// 20.(T).New style 2015 ES-6
    let tFunction = () =>{      //Fat arrow function
        console.log(`Hello from tFunction`);
        
    }
    tFunction();

// 21.(U).old way(1995-2014)
    // I.define
    function uFunction(){   //clasic style
        console.log('Hello from uFunction ');  
    }
    // II.call
    uFunction();


// 22.(V).New style 2015 ES-6
    const vFunction = () =>{      //Fat arrow function
        console.log("Hello from vFunction");
        
    }
    vFunction();

// 23.(W).old way(1995-2014)
    // I.define
    function wFunction(){   //clasic style
        console.log(`Hello from wFunction `);  
    }
    // II.call
    wFunction();


// 24.(X).New style 2015 ES-6
    var xFunction = () =>{      //Fat arrow function
        console.log('Hello from xFunction');
        
    }
    xFunction();

// 25.(Y).old way(1995-2014)
    // I.define
    function yFunction(){   //clasic style
        console.log("Hello from yFunction ");  
    }
    // II.call
    yFunction();


// 26.(z).New style 2015 ES-6
    let zFunction = () =>{      //Fat arrow function
        console.log(`Hello from zFunction`);
        
    }
    zFunction();




